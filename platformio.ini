[platformio]
default_envs = default
build_cache_dir = .cache
extra_configs = platformio.local.ini

[crosspoint]
version = 1.5.8

[base]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.37/platform-espressif32.zip
board = esp32-c3-devkitm-1
framework = arduino
monitor_speed = 115200
upload_speed = 921600
check_tool = cppcheck
; missingInclude (project headers) is suppressed alongside missingIncludeSystem: on a
; fresh CI checkout cppcheck has no resolved include paths, so it reports every
; project header as missing (~400 information-level lines) and fails the job.
check_flags = --enable=all --suppress=missingIncludeSystem --suppress=missingInclude --suppress=unusedFunction --suppress=unmatchedSuppression --suppress=*:*/.pio/* --inline-suppr
check_skip_packages = yes

board_upload.flash_size = 16MB
board_upload.maximum_size = 16777216
board_upload.offset_address = 0x10000

build_flags =
  -DARDUINO_USB_MODE=1
  -DARDUINO_USB_CDC_ON_BOOT=1
  -DEINK_DISPLAY_SINGLE_BUFFER_MODE=1
  -DDISABLE_FS_H_WARNING=1
  -DDESTRUCTOR_CLOSES_FILE=1
# https://libexpat.github.io/doc/api/latest/#XML_GE
  -DXML_GE=0
  -DXML_CONTEXT_BYTES=1024
  -std=gnu++2a
# Enable UTF-8 long file names in SdFat
  -DUSE_UTF8_LONG_NAMES=1
# Increase PNG scanline buffer to support up to 2048px wide images
# Default is (320*4+1)*2=2562, we need more for larger images
  -DPNG_MAX_BUFFERED_PIXELS=16416
  -DFREEINK_NET_WOLFSSL=1
  -DWOLFSSL_USER_SETTINGS
  -DWOLFSSL_OPTIONS_H
  -DWOLFSSL_CLIENT_EXAMPLE
  -DWOLFSSL_TLS13
# MEMFIX-PORT: single-precision ECC and RSA (sp_c32.c). Fast-math otherwise
# heap-allocates many FP_MAX_BITS temporaries during key exchange and RSA
# CertificateVerify. SP uses fixed-width workspaces; with WOLFSSL_SMALL_STACK,
# RSA-2048/4096 public operations need one temporary block of about 1.4/2.8KB.
# SP_SMALL trades large precomputed tables for flash, and SP_4096 covers the
# largest RSA public key accepted by the existing FP_MAX_BITS=8192 setting.
  -DWOLFSSL_HAVE_SP_ECC
  -DWOLFSSL_HAVE_SP_RSA
  -DWOLFSSL_SP_SMALL
  -DWOLFSSL_SP_4096
# SecureClient is the sole TLS fragment policy owner and requests 2KB records.
# This is a request, not a guarantee: peers may ignore the extension.
  -DHAVE_MAX_FRAGMENT
# Arduino-wolfSSL's user_settings.h enables TLS extensions, supported curves,
# HKDF, and RSA-PSS when WOLFSSL_TLS13 is set. Do not repeat those defines here:
# GCC reports every library translation unit as a macro redefinition.
  -DHAVE_FFDHE_2048
  -DHAVE_CURVE25519
  -DHAVE_SNI
  -Wno-bidi-chars
  -Wl,--wrap=panic_print_backtrace,--wrap=panic_abort,--wrap=bootloader_common_check_efuse_blk_validity
  -fno-exceptions
  ; Single firmware: CJK parsing/rendering and regional apps are always built.
  -DENABLE_CHINESE_VERSION=1

build_unflags =
  -std=gnu++11
  -fexceptions

build_src_filter =
  +<*>
  -<.git/>
  -<.svn/>

; Board configuration
board_build.flash_mode = dio
board_build.flash_size = 16MB
board_build.partitions = partitions.csv

extra_scripts =
  pre:scripts/patch_pioarduino_cache.py
  pre:scripts/patch_wolfssl.py
  pre:scripts/build_html.py
  pre:scripts/gen_i18n.py
  pre:scripts/git_branch.py
  pre:scripts/patch_jpegdec.py
  post:scripts/register_unit_tests_target.py

; Libraries
lib_deps =
  BatteryMonitor=symlink://freeink-sdk/libs/hardware/BatteryMonitor
  InputManager=symlink://freeink-sdk/libs/hardware/InputManager
  EInkDisplay=symlink://freeink-sdk/libs/display/FreeInkDisplay
  SDCardManager=symlink://freeink-sdk/libs/hardware/SDCardManager
  UsbMassStorage=symlink://freeink-sdk/libs/hardware/UsbMassStorage
  BoardConfig=symlink://freeink-sdk/libs/hardware/BoardConfig
  XteinkDetect=symlink://freeink-sdk/libs/hardware/XteinkDetect
  PowerManager=symlink://freeink-sdk/libs/hardware/PowerManager
  BleKeyboardHost=symlink://freeink-sdk/libs/network/BleKeyboardHost
  FrontlightManager=symlink://freeink-sdk/libs/hardware/FrontlightManager
  Rtc=symlink://freeink-sdk/libs/hardware/Rtc
  Imu=symlink://freeink-sdk/libs/hardware/Imu
  SecureNet=symlink://freeink-sdk/libs/network/SecureNet
  FreeInkUI=symlink://freeink-sdk/libs/ui/FreeInkUI
  Icons=symlink://freeink-sdk/libs/assets/Icons
  bblanchon/ArduinoJson @ 7.4.2
  ricmoo/QRCode @ 0.0.1
  bitbank2/PNGdec @ 1.1.6
  https://github.com/bitbank2/JPEGDEC.git#86282979224c8a32fd51e091ed5a35b0c699a52b
  links2004/WebSockets @ 2.7.3
  knolleary/PubSubClient @ ^2.8
  wolfssl/Arduino-wolfSSL @ 5.7.2

lib_ignore =
  BLE

; The standard firmware profiles reclaim heap by rebuilding the Arduino core.
; USB-OTG (FREEINK_CAP_USB_MSC) profiles like the X4 Pro and Paper Mono
; intentionally do not inherit this section: they need PioArduino's complete
; prebuilt TinyUSB component graph, which the custom core rebuild omits.
[firmware_tuned]
; MEMFIX-PORT: custom_sdkconfig heap reclamation (~32-37 KB). Rebuilds the
; Arduino core libs on first build (slower once, cached after; needs the CMake
; pin in platformio.local.ini on macOS).
;
; If an interrupted rebuild fails with "multiple definition of 'app_main'"
; (stale generated scaffold), clean it up with:
;   rm -rf .dummy CMakeLists.txt sdkconfig.default sdkconfig.defaults .pio/build/default
; Do NOT use `git clean -fdX` — it deletes platformio.local.ini.
custom_sdkconfig =
  ; Task stack right-sizing from measured high-water marks (heap block map +
  ; per-task stack audit, July 2026): esp_timer used ~0.8 KB of 8 KB across
  ; every capture; the FreeRTOS timer service used ~0.5 KB
  ; of 4 KB. Neither runs TLS or app code. ~7 KB back to the heap.
  CONFIG_ESP_TIMER_TASK_STACK_SIZE=4096
  CONFIG_FREERTOS_TIMER_TASK_STACK_DEPTH=2560
  ; Move the WiFi stack's non-critical hot paths out of IRAM into flash.
  ; On the C3, IRAM and DRAM share one SRAM pool, so the ~25-30 KB this
  ; frees lands directly in the heap — paid for with lower WiFi throughput
  ; during transfers (occasional sync/OTA use, not streaming: acceptable).
  ; IRAM cost is static, so the heap gain applies even with WiFi off.
  CONFIG_ESP_WIFI_IRAM_OPT=n
  CONFIG_ESP_WIFI_RX_IRAM_OPT=n
  ; Keep the Arduino wrappers for the removed cloud components (below) out of
  ; the core source list; all other bundled libraries default to enabled.
  CONFIG_ARDUINO_SELECTIVE_COMPILATION=y
  CONFIG_ARDUINO_SELECTIVE_RainMaker=n
  CONFIG_ARDUINO_SELECTIVE_Insights=n
  ; ESP_SR (speech recognition) only exists in the S3 core; its wrapper includes
  ; ESP_I2S.h, which the isolated core rebuild can't resolve. Unused here anyway,
  ; so drop it or the sticky env fails to build.
  CONFIG_ARDUINO_SELECTIVE_ESP_SR=n

; Drop unused cloud components from the core rebuild. esp_insights/rainmaker
; require embedded server certs the lib builder can't generate
; ("https_server.crt.S not found"); this firmware uses none of them.
custom_component_remove =
  espressif/esp_insights
  espressif/esp_rainmaker
  espressif/esp_diagnostics
  espressif/esp_diag_data_store
  espressif/esp_schedule
  espressif/esp_rcp_update
  espressif/esp_secure_cert_mgr
  espressif/cbor

; X3/X4 share the C3 controller and internal-RAM host in every firmware flavor.
[c3_hardware]
extends = base, firmware_tuned
lib_deps = ${c3_ble.lib_deps}
extra_scripts = ${c3_ble.extra_scripts}
custom_nimble_config = ${c3_ble.custom_nimble_config}
custom_sdkconfig =
  ${firmware_tuned.custom_sdkconfig}
  ${c3_ble.custom_sdkconfig}
build_flags =
  ${base.build_flags}
  ${c3_ble.build_flags}
  -DFREEINK_DEVICE_X4=1
  -DFREEINK_DEVICE_X3=1

[env:default]
extends = c3_hardware
build_flags =
  ${c3_hardware.build_flags}
  ; CROSSPOINT_VERSION is set by scripts/git_branch.py (includes branch + short SHA)
  -DENABLE_SERIAL_LOG
  -DCROSSPOINT_WAIT_FOR_USB_SERIAL
  -DLOG_LEVEL=2 ; Set log level to debug for development builds

[env:simulator]
platform = native
lib_ldf_mode = deep+
lib_compat_mode = off
build_src_filter =
  +<*>
  -<network/FirmwareFlasher.cpp>
  -<network/OtaBootSwitch.cpp>
  -<network/OtaUpdater.cpp>
  -<platform/skip_efuse_blk_check.c>
build_flags =
  -std=gnu++2a
  !sdl2-config --cflags --libs
  !python3 -c "import platform; print('-lcrypto' if platform.system() == 'Linux' else '')"
  -Wno-narrowing
  -DSIMULATOR
  -DFREEINK_DEVICE_X4CLASSIC=0
  -DCROSSPOINT_EMULATED=1
  -DCROSSPOINT_SIMULATOR_OTA_CHANNELS=1
  -DCROSSPOINT_SIMULATOR_PROJECT_WEBSERVER
  -DCROSSPOINT_VERSION=\"dev-simulator\"
  -DENABLE_CHINESE_VERSION=1
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=2
  -DEINK_DISPLAY_SINGLE_BUFFER_MODE=1
  -DMINIZ_NO_ZLIB_COMPATIBLE_NAMES=1
  -DXML_GE=0
  -DXML_CONTEXT_BYTES=1024
  -DUSE_UTF8_LONG_NAMES=1
  -DPNG_MAX_BUFFERED_PIXELS=16416
  -DDISABLE_FS_H_WARNING=1
  -DDESTRUCTOR_CLOSES_FILE=1
  -Isrc
  -Ilib/hal
build_unflags =
  -std=gnu++11
lib_ignore = hal, PNGdec, JPEGDEC, WebSockets
extra_scripts =
  pre:scripts/build_html.py
  pre:scripts/gen_i18n.py
  pre:scripts/git_branch.py
lib_deps =
  simulator=https://github.com/0x1abin/crosspoint-simulator.git#dacbbbcdc133a052f9122347429fdcb8cddf80af
  BleKeyboardHost=symlink://freeink-sdk/libs/network/BleKeyboardHost
  FreeInkUI=symlink://freeink-sdk/libs/ui/FreeInkUI
  Icons=symlink://freeink-sdk/libs/assets/Icons
  bblanchon/ArduinoJson @ 7.4.2
  ricmoo/QRCode @ 0.0.1
  knolleary/PubSubClient @ 2.8

; Common host dependencies; target-specific controller/allocator flags stay below.
[ble_host]
build_flags =
  -DFREEINK_CAP_BLE_HID_HOST=1
  -DFREEINK_BLE_HID_SHOW_UNNAMED_DEVICES=0
lib_deps =
  ${base.lib_deps}
  h2zero/NimBLE-Arduino @ 2.3.8
extra_scripts =
  ${base.extra_scripts}
  pre:scripts/patch_ble_keyboard_host.py

; C3 host/controller settings shared by development, release, RC and slim.
[c3_ble]
custom_nimble_config = src/platform/NimbleC3Config.h
build_flags = ${ble_host.build_flags}
lib_deps = ${ble_host.lib_deps}
extra_scripts =
  ${ble_host.extra_scripts}
  pre:scripts/configure_nimble_psram.py
  post:scripts/configure_c3_ble_controller.py
custom_sdkconfig =
  CONFIG_BT_ENABLED=y
  CONFIG_BT_BLUEDROID_ENABLED=n
  CONFIG_BT_NIMBLE_ENABLED=n
  CONFIG_BT_CONTROLLER_ONLY=y
  CONFIG_BT_CONTROLLER_ENABLED=y
  CONFIG_BT_CTRL_RUN_IN_FLASH_ONLY=y
  CONFIG_BT_CTRL_BLE_MAX_ACT=3
  CONFIG_BT_CTRL_BLE_ADV_REPORT_FLOW_CTRL_NUM=50
  CONFIG_BT_CTRL_ADV_DUP_FILT_MAX=10
  CONFIG_BLE_MESH=n

; S3 host settings shared by every hardware firmware flavor.
[s3_ble]
build_flags =
  ${ble_host.build_flags}
  -Wl,--wrap=xTaskCreatePinnedToCore
lib_deps = ${ble_host.lib_deps}
extra_scripts = ${ble_host.extra_scripts}

[s3_ble_psram]
lib_deps = ${s3_ble.lib_deps}
extra_scripts =
  ${s3_ble.extra_scripts}
  pre:scripts/configure_nimble_psram.py
build_flags =
  ${s3_ble.build_flags}
  -DCROSSPOINT_BLE_HOST_PSRAM=1

; Only custom-core S3 profiles use controller-only IDF Bluetooth. USB profiles
; retain the prebuilt dio_opi TinyUSB component graph.
[s3_ble_controller]
custom_sdkconfig =
  CONFIG_BT_ENABLED=y
  CONFIG_BT_BLUEDROID_ENABLED=n
  CONFIG_BT_NIMBLE_ENABLED=n
  CONFIG_BT_CONTROLLER_ONLY=y
  CONFIG_BT_CONTROLLER_ENABLED=y
  CONFIG_BT_NIMBLE_ROLE_PERIPHERAL=n
  CONFIG_BT_NIMBLE_ROLE_BROADCASTER=n
  CONFIG_BT_NIMBLE_MAX_CONNECTIONS=1
  CONFIG_BT_NIMBLE_MAX_CCCDS=2
  CONFIG_BT_NIMBLE_ATT_PREFERRED_MTU=23
  CONFIG_BT_NIMBLE_MSYS_1_BLOCK_COUNT=6
  CONFIG_BT_NIMBLE_MSYS_2_BLOCK_COUNT=6
  CONFIG_BT_NIMBLE_ACL_BUF_COUNT=6
  CONFIG_BT_NIMBLE_HCI_EVT_HI_BUF_COUNT=12
  CONFIG_BT_NIMBLE_TRANSPORT_ACL_FROM_LL_COUNT=6
  CONFIG_BT_NIMBLE_TRANSPORT_EVT_COUNT=12
  CONFIG_BT_NIMBLE_ATT_MAX_PREP_ENTRIES=4
  CONFIG_BT_CTRL_BLE_MAX_ACT=3
  CONFIG_BT_CTRL_BLE_ADV_REPORT_FLOW_CTRL_NUM=50
  CONFIG_BT_CTRL_ADV_DUP_FILT_MAX=10
  CONFIG_BLE_MESH=n

[env:simulator_x3]
extends = env:simulator
build_flags =
  ${env:simulator.build_flags}
  -DSIMULATOR_DEVICE_X3

[env:simulator_eego_a4]
extends = env:simulator
build_flags =
  ${env:simulator.build_flags}
  -DSIMULATOR_DEVICE_EEGO_A4

[env:simulator_murphy_m4]
extends = env:simulator
build_flags =
  ${env:simulator.build_flags}
  -DSIMULATOR_DEVICE_MURPHY_M4

[env:simulator_metalio]
extends = env:simulator
build_flags =
  ${env:simulator.build_flags}

[env:gh_release]
extends = c3_hardware
build_flags =
  ${c3_hardware.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}\"
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=1 ; Set log level to info for release builds

[env:gh_release_rc]
extends = c3_hardware
build_flags =
  ${c3_hardware.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-rc+${sysenv.CROSSPOINT_RC_HASH}\"
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=1 ; Set log level to info for release candidate builds

[env:slim]
extends = c3_hardware
build_flags =
  ${c3_hardware.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-slim\"
  ; serial output is disabled in slim builds to save space
  -UENABLE_SERIAL_LOG

; Shared release flags for all ESP32-S3 Nightly targets.
[s3_nightly]
build_flags =
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=1

; --- Seeed Sticky — ESP32-S3R8, 3.97" 800x480 SSD1677 + GT911 touch -----------
; Different MCU family than the C3 envs (one binary per family). The SDK
; auto-enables CAP_TOUCH (GT911) and the BQ27220 gauge for this device. BLE
; builds use the existing PSRAM heap for the Host only;
; the 48KB framebuffer remains in DRAM, same as X4.
;   pio run -e sticky -t upload
[sticky_hardware]
extends = base, firmware_tuned
board = esp32-s3-devkitc1-n16r8
board_build.mcu = esp32s3
lib_deps = ${s3_ble_psram.lib_deps}
extra_scripts = ${s3_ble_psram.extra_scripts}
custom_sdkconfig =
  ${firmware_tuned.custom_sdkconfig}
  ${s3_ble_controller.custom_sdkconfig}
build_flags =
  ${base.build_flags}
  ${s3_ble_psram.build_flags}
  -DFREEINK_DEVICE_STICKY=1

[env:sticky]
extends = sticky_hardware
build_flags =
  ${sticky_hardware.build_flags}
  -DENABLE_SERIAL_LOG
  -DCROSSPOINT_WAIT_FOR_USB_SERIAL
  -DLOG_LEVEL=2

[env:sticky-gh_release]
extends = sticky_hardware
build_flags =
  ${sticky_hardware.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}\"
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=1 ; Set log level to info for release builds

[env:sticky-gh_release_rc]
extends = sticky_hardware
build_flags =
  ${sticky_hardware.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-rc+${sysenv.CROSSPOINT_RC_HASH}\"
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=1 ; Set log level to info for release candidate builds

[env:sticky_nightly]
extends = sticky_hardware
build_flags =
  ${sticky_hardware.build_flags}
  ${s3_nightly.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-sticky-rc+${sysenv.CROSSPOINT_RC_HASH}\"

; --- Xteink X4 Pro — ESP32-S3, 16MB flash / 8MB PSRAM, SSD1677 + GT911 --------
; The SDK profile supplies the X4 Pro pinout, display, touch controller, and
; capacitive Home key. USB Serial/JTAG is the normal USB personality so
; PlatformIO can monitor and flash the reader. USB Drive starts MSC only for
; that File Transfer activity, then reboots back to this default on exit.
;   pio run -e x4pro -t upload
[x4pro_hardware]
extends = base
board = esp32-s3-devkitc1-n16r8
board_build.mcu = esp32s3
board_build.arduino.memory_type = dio_opi
; Use the prebuilt dio_opi Arduino variant: its TinyUSB component is enabled
; for CDC and MSC. This profile deliberately extends base rather than
; firmware_tuned, whose custom SDK rebuild omits that component.
lib_deps = ${s3_ble_psram.lib_deps}
extra_scripts = ${s3_ble_psram.extra_scripts}
build_flags =
  ${base.build_flags}
  ${s3_ble_psram.build_flags}
  -DFREEINK_DEVICE_X4PRO=1
  -DFREEINK_CAP_USB_MSC=1
  -DBOARD_HAS_PSRAM
  -DUSB_PRODUCT=\"CrossMux-X4Pro\"
  -DUSB_MANUFACTURER=\"CrossMux\"
  ; X4 Pro SD is native SDMMC (1-bit), mounted through SdFat's block-device API.
  -DUSE_BLOCK_DEVICE_INTERFACE=1

[env:x4pro]
extends = x4pro_hardware
build_flags =
  ${x4pro_hardware.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-x4pro\"
  -DENABLE_SERIAL_LOG
  -DCROSSPOINT_WAIT_FOR_USB_SERIAL
  -DLOG_LEVEL=2

[env:x4pro-gh_release]
extends = x4pro_hardware
build_flags =
  ${x4pro_hardware.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}\"
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=1

[env:x4pro-gh_release_rc]
extends = x4pro_hardware
build_flags =
  ${x4pro_hardware.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-rc+${sysenv.CROSSPOINT_RC_HASH}\"
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=1

[env:x4pro_nightly]
extends = x4pro_hardware
build_flags =
  ${x4pro_hardware.build_flags}
  ${s3_nightly.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-x4pro-rc+${sysenv.CROSSPOINT_RC_HASH}\"

; --- Xteink X4 Classic (X4C) ---------------------------------------------------
; Same ESP32-S3 board and display stack as the X4 Pro (SSD1677/UC8179/UC8279 auto-
; detected at boot) but buttons-only: no touchscreen and no frontlight, so the
; freed pins become four extra front buttons (two side page-turn keys + four bottom
; keys). Native 1-bit SDMMC + USB-MSC file transfer as on the X4 Pro; drops the
; frontlight flag. See freeink-sdk/docs/xteink-x4c-support.md.
;   pio run -e x4c -t upload
[x4c_hardware]
extends = base
board = esp32-s3-devkitc1-n16r8
board_build.mcu = esp32s3
board_build.arduino.memory_type = dio_opi
lib_deps = ${s3_ble_psram.lib_deps}
extra_scripts = ${s3_ble_psram.extra_scripts}
build_flags =
  ${base.build_flags}
  ${s3_ble_psram.build_flags}
  -DFREEINK_DEVICE_X4CLASSIC=1
  -DFREEINK_CAP_USB_MSC=1
  -DBOARD_HAS_PSRAM
  -DUSB_PRODUCT=\"CrossMux-X4Classic\"
  -DUSB_MANUFACTURER=\"CrossMux\"
  -DUSE_BLOCK_DEVICE_INTERFACE=1

[env:x4c]
extends = x4c_hardware
build_flags =
  ${x4c_hardware.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-x4c\"
  -DENABLE_SERIAL_LOG
  -DCROSSPOINT_WAIT_FOR_USB_SERIAL
  -DLOG_LEVEL=2

[env:x4c-gh_release]
extends = x4c_hardware
build_flags =
  ${x4c_hardware.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}\"
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=1 ; Set log level to info for release builds

[env:x4c-gh_release_rc]
extends = x4c_hardware
build_flags =
  ${x4c_hardware.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-rc+${sysenv.CROSSPOINT_RC_HASH}\"
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=1

[env:x4c_nightly]
extends = x4c_hardware
build_flags =
  ${x4c_hardware.build_flags}
  ${s3_nightly.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-x4c-rc+${sysenv.CROSSPOINT_RC_HASH}\"

; --- EEGO A4 — ESP32-S3 N16R8, 768x552 UC8279C + GSLX680 --------------------
; Experimental hardware target distributed through the Nightly channel only.
[eego_a4_hardware]
extends = base, firmware_tuned
board = esp32-s3-devkitc1-n16r8
board_build.mcu = esp32s3
lib_deps = ${s3_ble_psram.lib_deps}
extra_scripts = ${s3_ble_psram.extra_scripts}
build_flags =
  ${base.build_flags}
  ${s3_ble_psram.build_flags}
  -DFREEINK_DEVICE_EEGO_A4=1
  -DBOARD_HAS_PSRAM
; WeRead's network steps (TLS handshake + JSON decode) run on the Arduino
; loop task; the 8KB default stack overflows there (observed crashes entering
; WeRead on the A4). Give the loop task 16KB like the render task.
custom_sdkconfig =
  ${firmware_tuned.custom_sdkconfig}
  CONFIG_ARDUINO_LOOP_STACK_SIZE=16384
  ${s3_ble_controller.custom_sdkconfig}

[env:eego_a4]
extends = eego_a4_hardware
build_flags =
  ${eego_a4_hardware.build_flags}
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=2

[env:eego_a4_nightly]
extends = eego_a4_hardware
build_flags =
  ${eego_a4_hardware.build_flags}
  ${s3_nightly.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-eego-a4-rc+${sysenv.CROSSPOINT_RC_HASH}\"

; --- Murphy M4 — ESP32-S3 N16R8, 800x480 SSD1677 + FT6336 -------------------
; Experimental target distributed through the Nightly channel only. SD uses
; native 4-bit SDMMC.
[murphy_m4_hardware]
extends = base
board = esp32-s3-devkitc1-n16r8
board_build.mcu = esp32s3
board_build.arduino.memory_type = dio_opi
lib_deps = ${s3_ble_psram.lib_deps}
extra_scripts = ${s3_ble_psram.extra_scripts}
build_flags =
  ${base.build_flags}
  ${s3_ble_psram.build_flags}
  -DFREEINK_DEVICE_MURPHY_M4=1
  -DFREEINK_CAP_USB_MSC=1
  -DBOARD_HAS_PSRAM
  -DUSB_PRODUCT=\"CrossMux-Murphy-M4\"
  -DUSB_MANUFACTURER=\"CrossMux\"
  -DUSE_BLOCK_DEVICE_INTERFACE=1

[env:murphy_m4]
extends = murphy_m4_hardware
build_flags =
  ${murphy_m4_hardware.build_flags}
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=2

[env:murphy_m4_nightly]
extends = murphy_m4_hardware
build_flags =
  ${murphy_m4_hardware.build_flags}
  ${s3_nightly.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-murphy-m4-rc+${sysenv.CROSSPOINT_RC_HASH}\"

; Shared physical-button sound feedback capability. A board opts in only after
; its speaker path and level calibration have passed hardware acceptance.
[sound_feedback_hardware]
extends = base
board_build.embed_files =
  assets/sounds/select.wav
  assets/sounds/tap.wav
build_flags =
  ${base.build_flags}
  -DCROSSPOINT_CAP_SOUND_FEEDBACK=1
lib_deps =
  ${base.lib_deps}
  AudioManager=symlink://freeink-sdk/libs/hardware/AudioManager

; --- Waveshare ESP32-S3 ePaper 3.97 — SSD1677 + AXP2101 --------------------
; Experimental target. The reading core uses 4-bit SDMMC, PCF85063A RTC, and
; the four physical buttons. ES8311/NS4150B audio is limited to immediate physical
; button feedback with Off/Low/Medium/High levels; IMU and environmental sensors stay out.
[waveshare_epaper_397_hardware]
extends = sound_feedback_hardware
board = esp32-s3-devkitc1-n16r8
board_build.mcu = esp32s3
; Keep this profile on the prebuilt TinyUSB-enabled variant so USB Drive can
; switch the shared S3 PHY from Serial/JTAG to MSC and back.
board_build.arduino.memory_type = dio_opi
lib_deps =
  ${s3_ble_psram.lib_deps}
  AudioManager=symlink://freeink-sdk/libs/hardware/AudioManager
extra_scripts = ${s3_ble_psram.extra_scripts}
build_flags =
  ${sound_feedback_hardware.build_flags}
  ${s3_ble_psram.build_flags}
  -DFREEINK_DEVICE_WAVESHARE_EPAPER_397=1
  -DFREEINK_CAP_USB_MSC=1
  -DBOARD_HAS_PSRAM
  -DUSB_PRODUCT=\"CrossMux-Waveshare-ePaper397\"
  -DUSB_MANUFACTURER=\"CrossMux\"
  -DUSE_BLOCK_DEVICE_INTERFACE=1

[env:waveshare_epaper_397]
extends = waveshare_epaper_397_hardware
build_flags =
  ${waveshare_epaper_397_hardware.build_flags}
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=2

[env:waveshare_epaper_397_nightly]
extends = waveshare_epaper_397_hardware
build_flags =
  ${waveshare_epaper_397_hardware.build_flags}
  ${s3_nightly.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-waveshare-epaper-397-rc+${sysenv.CROSSPOINT_RC_HASH}\"

; --- M5Stack Paper Mono — ESP32-S3, SSD1677 800x480 + FT6336 touch + frontlight
; Power/reset plumbing runs through the M5PM1 PMIC and M5IOE1 expander; the SDK
; sequences them itself. Touch, frontlight, RTC, and other capabilities are
; selected by the Paper Mono BoardConfig profile.
; Extends base (not firmware_tuned): USB Drive needs the prebuilt dio_opi
; Arduino variant, whose TinyUSB component is enabled for CDC and MSC.
;   pio run -e papermono -t upload
[papermono_hardware]
extends = base
board = esp32-s3-devkitc1-n16r8
board_build.mcu = esp32s3
board_build.arduino.memory_type = dio_opi
lib_deps = ${s3_ble_psram.lib_deps}
extra_scripts = ${s3_ble_psram.extra_scripts}
build_flags =
  ${base.build_flags}
  ${s3_ble_psram.build_flags}
  -DFREEINK_DEVICE_PAPERMONO=1
  -DFREEINK_CAP_USB_MSC=1
  ; The SSD1677 grayscale target planes are batched in PSRAM.
  -DBOARD_HAS_PSRAM
  -DUSB_PRODUCT=\"CrossMux-PaperMono\"
  -DUSB_MANUFACTURER=\"CrossMux\"
  ; Native 1-bit SDMMC is mounted through SdFat's block-device interface.
  -DUSE_BLOCK_DEVICE_INTERFACE=1

[env:papermono]
extends = papermono_hardware
build_flags =
  ${papermono_hardware.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-papermono\"
  -DENABLE_SERIAL_LOG
  -DCROSSPOINT_WAIT_FOR_USB_SERIAL
  -DLOG_LEVEL=2

[env:papermono-gh_release]
extends = papermono_hardware
build_flags =
  ${papermono_hardware.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}\"
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=1

[env:papermono-gh_release_rc]
extends = papermono_hardware
build_flags =
  ${papermono_hardware.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-rc+${sysenv.CROSSPOINT_RC_HASH}\"
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=1

[env:papermono_nightly]
extends = papermono_hardware
build_flags =
  ${papermono_hardware.build_flags}
  ${s3_nightly.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-papermono-rc+${sysenv.CROSSPOINT_RC_HASH}\"

; --- Metalio E-Ink 4 — ESP32-S3, SSD1677 800x480 + CST816S touch + TCA9555 IO expander
[metalio_hardware]
extends = base
board = esp32-s3-devkitc1-n16r8
board_build.mcu = esp32s3
board_build.arduino.memory_type = dio_opi
lib_deps = ${s3_ble_psram.lib_deps}
extra_scripts = ${s3_ble_psram.extra_scripts}
build_flags =
  ${base.build_flags}
  ${s3_ble_psram.build_flags}
  -DFREEINK_DEVICE_METALIO=1
  -DBOARD_HAS_PSRAM
  -DUSB_PRODUCT=\"CrossMux-Metalio\"
  -DUSB_MANUFACTURER=\"CrossMux\"
  -DUSE_BLOCK_DEVICE_INTERFACE=1

[env:metalio]
extends = metalio_hardware
build_flags =
  ${metalio_hardware.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-metalio\"
  -DENABLE_SERIAL_LOG
  -DCROSSPOINT_WAIT_FOR_USB_SERIAL
  -DLOG_LEVEL=2

[env:metalio-gh_release]
extends = metalio_hardware
build_flags =
  ${metalio_hardware.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}\"
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=1

[env:metalio-gh_release_rc]
extends = metalio_hardware
build_flags =
  ${metalio_hardware.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-rc+${sysenv.CROSSPOINT_RC_HASH}\"
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=1
